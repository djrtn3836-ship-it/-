# ============================================================
# 📦 Stock Analyzer v10.0 - AI 퀀트 시스템 의존성
# V10 DDD 아키텍처 기준
# ============================================================
# 🔥 설치 명령어: pip install -r requirements.txt
# ============================================================

# ---- 기반 라이브러리 ----
aiohttp>=3.9.0
aiohttp[speedups]>=3.9.0  # 속도 최적화
async-timeout>=4.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
pytimekr>=0.4.0  # 한국 공휴일

# ---- V10 신규 의존성 ----
pydantic>=2.0.0          # config/schema.py - 설정 스키마
cachetools>=7.0.0        # infrastructure/market_data 캐싱, orchestrator/feature_store L1 캐시

# ---- 데이터 처리 ----
pandas>=2.0.0
numpy>=1.24.0
polars>=0.19.0
scipy>=1.11.0

# ---- WebSocket / 네트워크 ----
websockets>=13.0

# ---- Telegram ----
python-telegram-bot>=20.0

# ---- 머신러닝 / AI ----
scikit-learn>=1.3.0
xgboost>=2.0.0
torch>=2.0.0
transformers>=4.30.0

# ---- 스케줄러 ----
APScheduler>=3.10.0

# ---- DB ----
aiosqlite>=0.19.0
asyncpg>=0.29.0   # Phase 4: PostgreSQL (DATABASE_URL 설정 시 활성화, 미설정 시 무해)

# ---- 캐시 (Session 27) ----
redis>=5.0.0      # infrastructure/cache/redis_cache.py - REDIS_URL 설정 시 활성화, 미설정 시 무해
hiredis>=2.2.0    # redis 응답 파싱 C 가속 (선택, 미설치 시 순수 Python 파서로 자동 대체)

# ---- 유틸리티 ----
pytest>=7.0.0
pytest-asyncio>=0.23.0   # pyproject.toml asyncio_mode 설정 실사용 확인
yfinance>=0.2.28

# ---- 코드 품질 ----
pyflakes>=3.0.0
mypy>=1.0.0
optuna>=3.0.0   # application/analysis/hyperparameter_tuner.py 실사용 확인

# ============================================================
# 📌 추가 설치 권장 (선택)
# ============================================================
# GPU 가속 사용 시: pip install torch --index-url https://download.pytorch.org/whl/cu118
# KoBERT 사용 시: pip install git+https://github.com/SKTBrain/KoBERT.git
# PostgreSQL 사용 시: pip install asyncpg
# Redis 캐시 사용 시: pip install redis hiredis (docker-compose up -d로 로컬 Redis 실행)
# ============================================================
